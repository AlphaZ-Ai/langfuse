export X_API_KEY=
export BASE_URL=
export TEST_BASE_URL=
export LANGFUSE_PUBLIC_KEY=
export LANGFUSE_SECRET_KEY=
export LANGFUSE_BASE_URL=
export LANGFUSE_TRACING_ENVIRONMENT=
export NEXTAUTH_URL=
export GITHUB_TOKEN=
export GADM_EVAL_PATH=
export GADM_MODELS_PATH=
# UID/GID the gadm-webui container runs as, so scenarios written back
# through the bind-mounted tests/ volume are owned by you, not root.
# Set to your host user: `id -u` / `id -g`.
export GADM_WEBUI_UID=1000
export GADM_WEBUI_GID=1000
