export X_API_KEY=
export BASE_URL=
export TEST_BASE_URL=
export LANGFUSE_PUBLIC_KEY=
export LANGFUSE_SECRET_KEY=
export LANGFUSE_BASE_URL=
export LANGFUSE_TRACING_ENVIRONMENT=
export NEXTAUTH_URL=
export GITHUB_TOKEN=

# Enable UI batch export (CSV/JSON) of traces. minio is published on host port
# 9090; set the external endpoint to a browser-reachable address.
export LANGFUSE_S3_BATCH_EXPORT_ENABLED=true
export LANGFUSE_S3_BATCH_EXPORT_EXTERNAL_ENDPOINT=http://localhost:9090